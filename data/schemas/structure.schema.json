{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://roaringlions.dev/schemas/structure.schema.json",
  "title": "Structure catalogue",
  "description": "Building types. A structure is a contiguous run of same-symbol tiles on a map: it blocks movement, absorbs fire, shelters a garrison, and leaves passable rubble when it falls. Masonry is not armour — how much a round hurts a building is a property of the round, not a penetration roll.",
  "type": "object",
  "required": ["types"],
  "additionalProperties": false,
  "properties": {
    "version": { "type": "integer", "minimum": 1 },
    "types": {
      "type": "object",
      "minProperties": 1,
      "additionalProperties": {
        "type": "object",
        "required": ["id", "symbol", "hp_per_tile"],
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string", "pattern": "^[a-z0-9_]+$" },
          "name": { "type": "string" },
          "symbol": {
            "type": "string",
            "minLength": 1,
            "maxLength": 1,
            "description": "Map legend character. Must not collide with terrain symbols . 1 2 3."
          },
          "hp_per_tile": {
            "type": "number",
            "minimum": 1,
            "description": "Structural integrity per tile of footprint: a bigger building is a tougher one."
          },
          "garrison_slots": {
            "type": "integer",
            "minimum": 0,
            "default": 0,
            "description": "How many units can fight from inside. 0 = not garrisonable."
          },
          "rubble_cover": { "type": "integer", "minimum": 0, "maximum": 3, "default": 2 },
          "height_px": { "type": "number", "minimum": 0, "description": "Presentation only." },
          "color": { "type": "string", "description": "Presentation only: palette key." }
        }
      }
    }
  }
}
